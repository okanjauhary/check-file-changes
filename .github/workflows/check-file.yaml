name: Checking file changes

on:
  push:
    branches:
      - main
    paths:
      - 'test_directory/**'

jobs:
  node-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Linting
        run: |
          echo "Linting file in test_directory"
